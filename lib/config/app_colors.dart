import 'package:flutter/material.dart';

class AppColors {
  // Primary — Warm Amber Gold (sunshine, fortune, coins)
  static const primary = Color(0xFFF5A623);
  static const primaryDark = Color(0xFFD4881A);
  static const primaryLight = Color(0xFFFFCF6B);
  static const primarySurface = Color(0xFFFFF8E1);

  // Secondary — Lucky Emerald (four-leaf clover, growth, hope)
  static const secondary = Color(0xFF34C98E);
  static const secondaryDark = Color(0xFF22A875);
  static const secondarySurface = Color(0xFFE8FBF3);

  // Accent — Sky Blue (open possibilities)
  static const accent = Color(0xFF5B9CF6);
  static const accentSurface = Color(0xFFEBF3FF);

  // Backgrounds
  static const bgPage = Color(0xFFFFFDF7);
  static const bgCard = Color(0xFFFFFFFF);
  static const bgSecondary = Color(0xFFF8F9FA);

  // Text
  static const textPrimary = Color(0xFF1E2328);
  static const textSecondary = Color(0xFF6B7480);
  static const textHint = Color(0xFFB0B8C1);

  // Status
  static const success = Color(0xFF34C98E);
  static const warning = Color(0xFFF5A623);
  static const error = Color(0xFFFF4757);
  static const info = Color(0xFF5B9CF6);

  // Prize tiers (roulette wheel)
  static const prizeJackpot = Color(0xFFFF4757);
  static const prize100p = Color(0xFFA55EEA);
  static const prizeLarge = Color(0xFFF5A623);
  static const prizeMed = Color(0xFF34C98E);
  static const prizeSmall = Color(0xFF5B9CF6);
  static const prizeMiss = Color(0xFFBDC3C7);

  // Gradient helpers
  static const goldGradient = [Color(0xFFF5A623), Color(0xFFFFCF6B), Color(0xFFF5A623)];
  static const appBarGradient = [Color(0xFFF5A623), Color(0xFFFFB84D)];
  static const heroGradient = [Color(0xFFFFF8E1), Color(0xFFFFFDF7)];
}
